package core;

public interface CalculatorService {
//    Assume this is a Cloud service i.e., CalculatorService is a cloud service
    int add(int i, int j);
    int subtract(int i, int j);
    int divide(int i, int j);
    int multiply(int i, int j);
}
